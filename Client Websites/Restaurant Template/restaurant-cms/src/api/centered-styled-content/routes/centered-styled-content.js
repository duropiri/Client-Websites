'use strict';

/**
 * centered-styled-content router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::centered-styled-content.centered-styled-content');
